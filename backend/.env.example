# Mongo connection string (replace with your own credentials)
MONGODB_URI=mongodb+srv://username:password@cluster0.mongodb.net/gofoodmern?retryWrites=true&w=majority

# Secret key used for signing JWT tokens
JWT_SECRET=change-this-secret

# Optional: override the Express port (defaults to 5000)
PORT=5000
